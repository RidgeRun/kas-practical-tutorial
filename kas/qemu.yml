header:
  version: 11
  includes:
    - kas/base.yml

machine: qemux86-64

local_conf_header:
  debug: |
    EXTRA_IMAGE_FEATURES += "debug-tweaks"
  parallelism: |
    BB_NUMBER_THREADS = "16"
    PARALLEL_MAKE = "-j16"
